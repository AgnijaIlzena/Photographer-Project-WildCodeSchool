{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/style/styleConnect.css">
{% endblock %}

{% block body %}

        <div class="container">
            <div class="row">
                <div class="col-sm-12-8">
                    {# Carousel #}
                    <div id="carouselExample" class="carousel slide"  data-bs-ride="carousel">
                        <div class="carousel-inner" role="listbox">
                            {% for photo in galery.photos %}
                                <div class="carousel-item {% if loop.index == 1 %} active {% endif %}">
                                    <img class="d-block w-100" src="{{ ('/uploads/{{ photo.path }}')|imagine_filter('carousel') }}">
                                </div>
                            {% endfor %}
                        </div>

                        <a class="carousel-control-prev" href="#carouselExample" role="button" data-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="sr-only">Previous</span>
                        </a>
                        <a class="carousel-control-next" href="#carouselExample" role="button" data-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="sr-only">Next</span>
                        </a>
                    </div>
                </div>

                {# Biography #}
                <div class="col-md-4 mt-5">
                    <img src="/images/photographer2.jpg" height="90%" width="95%" alt="photographer" />
                </div>
                <div class="col-md-8 mt-5">
                    <h1>A propos de moi</h1>
                    <p>L'univers de la photographie m'a toujours attiré depuis le plus jeune âge.</p>
                    <p>La photographie photographie permet de témoigner de l'éphémère à l'instant du déclic</p>
                    <p>Passionné par mon travail de photographe, je me déplace sur toute la France
                        et à l'étranger.</p>
                    <p>N'hésitez pas à me contacter pour vos projets photographiques.
                        Je me consacrerais totalement à vous en prenant en compte vos différents besoins.</p>

                    <p> Accédez à mes photos : <a href="{{ path("galery_index") }}">Galeries</a> </p>
                    </div>
                </div>
            </div>
        </div>

{% endblock %}
