{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ photo.path }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ photo.number }}</td>
            </tr>
        </tbody>
    </table>
    <div >
        <img  style="width: 100px; height: 100px" src="{{ asset( photo.path )}}">
    </div>

    <a href="{{ path('photo_index') }}">back to list</a>

    <a href="{{ path('photo_edit', {'id': photo.id}) }}">edit</a>

    {{ include('photo/_delete_form.html.twig') }}
{% endblock %}
