{% extends 'base.html.twig' %}

{% block title %}Edit Galery{% endblock %}
{% block header %}{{ include('adminDashboard/admin/_adminNavBar.html.twig') }}{% endblock %}

{% block body %}
<div class="container mt-5">
<section class="jumbotron" style="background-color: white;">
    <div class="card mb-3">
        <div class="card-header bg-warning" style="width:95%; height: 10%; position: relative; bottom:20px; left:20px;" >
                <h4 class="card-title text-light text-uppercase">edit gallery <span class="pr-3">:</span>{{ galery.title }} </h4>
        </div>
        {{ include('adminDashboard/galery/_form.html.twig', {'button_label': 'Update galery'}) }}
    </div>
        {{ include('adminDashboard/galery/_delete_form.html.twig') }}
    {#  <a href="{{ path('admin_galery_index') }}">back to list</a> #}
</section>
</div>
{% endblock %}
