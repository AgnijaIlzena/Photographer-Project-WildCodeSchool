<form  class="col-lg-12 text-center " method="post" action="{{ path('photo_delete', {'id': photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photo.id) }}">
    <button class="col btn btn-outline-danger" style="font-size: x-small; max-width: fit-content">delete photo</button>
</form>

